package descriptions

const RfcShort = "react: Creates a React component, test file, storybook, and presentation hook"

const RfcLong = `Creates a React component, Jest test file, storybook, and presentation hook

example:

es rfc NewComponent
`
